<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MB DIGITAL | MERCHBOY Creative Studio</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            width: 90%;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .hero {
            text-align: center;
            color: white;
            animation: fadeInUp 1s ease-out;
        }

        .logo-section {
            margin-bottom: 30px;
            animation: float 3s ease-in-out infinite;
        }

        .logo {
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 900;
            letter-spacing: -2px;
            background: linear-gradient(45deg, #fff, #ffd700, #fff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 30px rgba(255, 215, 0, 0.3);
            line-height: 1.1;
        }

        .tagline {
            font-size: clamp(1rem, 3vw, 1.5rem);
            color: rgba(255, 255, 255, 0.9);
            margin-top: 15px;
            font-weight: 300;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .description {
            font-size: clamp(1rem, 2.5vw, 1.25rem);
            color: rgba(255, 255, 255, 0.85);
            margin: 30px auto;
            max-width: 600px;
            line-height: 1.6;
        }

        .cta-container {
            margin-top: 50px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
            max-width: 1100px;
            margin-left: auto;
            margin-right: auto;
        }

        .card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            transform: translateY(0);
            transition: all 0.3s ease;
            animation: fadeInUp 1s ease-out 0.3s both;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.4);
        }

        .card:nth-child(2) {
            animation-delay: 0.5s;
        }

        .shop-card {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 60px 40px;
            text-decoration: none;
            position: relative;
            overflow: hidden;
        }

        .shop-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            animation: pulse 3s ease-in-out infinite;
        }

        .shop-logo {
            width: 200px;
            height: 200px;
            margin-bottom: 20px;
            filter: drop-shadow(0 10px 30px rgba(0,0,0,0.3));
            transition: transform 0.3s ease;
            position: relative;
            z-index: 1;
        }

        .shop-card:hover .shop-logo {
            transform: scale(1.1) rotate(5deg);
        }

        .shop-text {
            color: white;
            font-size: 1.8rem;
            font-weight: 800;
            text-align: center;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
            position: relative;
            z-index: 1;
        }

        .shop-subtext {
            color: rgba(255,255,255,0.9);
            font-size: 1rem;
            text-align: center;
            margin-top: 10px;
            position: relative;
            z-index: 1;
        }

        @keyframes pulse {
            0%, 100% {
                transform: translate(0, 0) scale(1);
                opacity: 0.3;
            }
            50% {
                transform: translate(-10%, -10%) scale(1.1);
                opacity: 0.5;
            }
        }

        .card-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .card-icon {
            font-size: 3rem;
            animation: bounce 2s ease-in-out infinite;
        }

        .card-title {
            font-size: 1.8rem;
            font-weight: 800;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .shop-title {
            background: linear-gradient(135deg, #f093fb, #f5576c);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .card-description {
            color: #555;
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 25px;
        }

        .card-button {
            display: inline-block;
            padding: 18px 40px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
            position: relative;
            overflow: hidden;
        }

        .shop-button {
            background: linear-gradient(135deg, #f093fb, #f5576c);
            box-shadow: 0 10px 30px rgba(245, 87, 108, 0.4);
        }

        .card-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .card-button:hover::before {
            left: 100%;
        }

        .card-button:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.6);
        }

        .shop-button:hover {
            box-shadow: 0 15px 40px rgba(245, 87, 108, 0.6);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
            margin-top: 25px;
            padding-top: 25px;
            border-top: 2px solid #f0f0f0;
        }

        .feature {
            text-align: center;
            color: #666;
            font-size: 0.9rem;
        }

        .feature-icon {
            font-size: 1.5rem;
            margin-bottom: 5px;
        }

        .floating-shapes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            animation: float-shape 20s ease-in-out infinite;
        }

        .shape1 { width: 80px; height: 80px; top: 20%; left: 10%; animation-delay: 0s; }
        .shape2 { width: 120px; height: 120px; top: 60%; right: 15%; animation-delay: 2s; }
        .shape3 { width: 60px; height: 60px; bottom: 20%; left: 20%; animation-delay: 4s; }
        .shape4 { width: 100px; height: 100px; top: 40%; right: 30%; animation-delay: 6s; }

        footer {
            text-align: center;
            color: rgba(255, 255, 255, 0.7);
            margin-top: 60px;
            font-size: 0.9rem;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20px);
            }
        }

        @keyframes float-shape {
            0%, 100% {
                transform: translate(0, 0) scale(1);
                opacity: 0.1;
            }
            50% {
                transform: translate(50px, -50px) scale(1.2);
                opacity: 0.2;
            }
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        @media (max-width: 768px) {
            .cta-container {
                grid-template-columns: 1fr;
            }
            
            .card {
                padding: 30px 20px;
            }
            
            .features {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</head>
<body>
    <div class="floating-shapes">
        <div class="shape shape1"></div>
        <div class="shape shape2"></div>
        <div class="shape shape3"></div>
        <div class="shape shape4"></div>
    </div>

    <div class="container">
        <div class="hero">
            <div class="logo-section">
                <div class="logo">MB DIGITAL</div>
                <div class="tagline">Creative Studio</div>
            </div>
            
            <p class="description">
                Where creativity meets digital innovation. Building brands, characters, and experiences that inspire.
            </p>

            <div class="cta-container">
                <!-- Shop Card with Logo -->
                <a href="https://merchboy.shop" target="_blank" rel="noopener" class="card shop-card">
                    <img src="merchboy-logo.png" alt="MERCHBOY Logo" class="shop-logo">
                    <div class="shop-text">MERCHBOY.SHOP</div>
                    <div class="shop-subtext">Fresh drops • Quality gear • Free shipping</div>
                </a>

                <!-- Coloring Studio Card -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-icon">🎨</div>
                        <div class="card-title">Coloring Studio</div>
                    </div>
                    
                    <p class="card-description">
                        Jump into our interactive digital coloring book featuring 21 pages of MERCHBOY characters, Valentine's scenes, and abstract designs. Perfect for all ages!
                    </p>
                    
                    <a href="merchboy-coloring-ultimate-v2.html" class="card-button">
                        Start Coloring Now →
                    </a>

                    <div class="features">
                        <div class="feature">
                            <div class="feature-icon">📱</div>
                            <div>Mobile Ready</div>
                        </div>
                        <div class="feature">
                            <div class="feature-icon">🎯</div>
                            <div>21 Pages</div>
                        </div>
                        <div class="feature">
                            <div class="feature-icon">🎨</div>
                            <div>48 Colors</div>
                        </div>
                        <div class="feature">
                            <div class="feature-icon">⚡</div>
                            <div>Smart Fill</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <footer>
            <p>© 2026 MB DIGITAL • Part of the MERCHBOY Brand Family</p>
        </footer>
    </div>
</body>
</html>
